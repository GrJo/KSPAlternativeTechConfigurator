ATC_SETTINGS
{
	name = default

	//Key combinations (primarily used for debugging purposes)
	//Modifiers: & = Alt, ^ = Control, % = Command/Windows key, # = Shift
	debugDumpKeyCombo = ^D
	reloadKeyCombo = ^R

	ACTIVE_TECH_TREE
	{
		name = stock
	}
}
